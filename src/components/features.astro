---
import Link from "@components/ui/link.astro";
// Remove old astro-i18next imports
// import { localizePath } from "astro-i18next";
// import { t } from "i18next";

// Add new built-in i18n imports
import { getLangFromUrl, useTranslations } from "../i18n/utils";
import { getRelativeLocaleUrl } from "astro:i18n";

// @ts-ignore
import { Icon } from "astro-icon/components";
import React from 'react';
import { Search, Link, Download } from 'lucide-react';

// Get current language and create translation function
const safeUrl = Astro.url || new URL('/', Astro.site || 'http://localhost:4321');
const lang = getLangFromUrl(safeUrl);
const t = useTranslations(lang);

// Helper function to create localized paths (if needed)
function localizePath(path: string) {
	if (!path || (path && path.startsWith('http')) || path === '#') return path;
	return getRelativeLocaleUrl(lang, path);
}
const StepperComponent = () => {
  const steps = [
    {
      id: 1,
      title: "Find a TT",
      description: "Open the TikTok app on your mobile device. Scroll to find the video you'd like to save. Play the video to confirm it's the right one.",
      icon: <Search className="w-8 h-8" />
    },
    {
      id: 2,
      title: "Copy the link",
      description: "Tap the Share button located on the right side of the screen. Select Copy link from the menu. Check your clipboard to ensure the URL is copied correctly.",
      icon: <Link className="w-8 h-8" />
    },
    {
      id: 3,
      title: "Save TikTok",
      description: "Go to the sss TikTok download without watermark service and paste the copied link into the text field at the top. Click 'Save' to start the video download.",
      icon: <Download className="w-8 h-8" />
    }
  ];

 
---

 return (
    <div className="min-h-screen bg-gradient-to-br from-purple-600 via-purple-700 to-indigo-800 p-6 flex items-center justify-center">
      <div className="w-full max-w-2xl">
        {/* Header */}
        <div className="text-center mb-12">
          <h1 className="text-3xl md:text-4xl font-bold text-white mb-4">
            How to download TikTok without watermark?
          </h1>
        </div>

        {/* Steps */}
        <div className="space-y-8">
          {steps.map((step, index) => (
            <div key={step.id} className="flex items-start space-x-6">
              {/* Step Icon/Number */}
              <div className="flex-shrink-0">
                <div className="w-16 h-16 bg-white/20 backdrop-blur-sm rounded-2xl flex items-center justify-center text-white border border-white/30">
                  {step.icon}
                </div>
              </div>

              {/* Step Content */}
              <div className="flex-1 pt-2">
                <h3 className="text-2xl font-bold text-white mb-2">
                  {step.title}
                </h3>
                <p className="text-white/90 text-lg leading-relaxed">
                  {step.description}
                </p>
              </div>

              {/* Connector Line */}
              {index < steps.length - 1 && (
                <div className="absolute left-8 mt-20 w-0.5 h-8 bg-white/30"></div>
              )}
            </div>
          ))}
        </div>

        {/* Bottom decoration */}
        <div className="mt-12 text-center">
          <div className="inline-flex space-x-2">
            {steps.map((_, index) => (
              <div 
                key={index}
                className="w-3 h-3 bg-white/30 rounded-full"
              ></div>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default StepperComponent;

<div class="relative p-4">
      <div class="max-w-screen-xl mx-auto">
            <div class="rounded-b lg:rounded-b-none lg:rounded-r flex flex-col justify-between leading-normal">
				<section id="about" class="relative overflow-hidden">
					<div class="absolute inset-0 w-full h-full"></div>
    					<div class="container max-w-7xl mx-auto px-4 relative z-10">
      						<div class="text-center mb-16">
            					<h2 class="text-2xl md:text-3xl font-bold text-gray-900 dark:text-white">Tiktok Downloader <span class="text-orange-400 dark:text-orange-400">Download Tiktok Videos</span>Without Watermark</h2>
            						<div class="w-24 h-1 mx-auto bg-orange-400"></div>
            						<p class="mt-4 mx-auto text-lg text-gray-900 dark:text-white">Tiktokio is fastest <a href="/">tiktok downloader</a> to download tiktok video. This tiktok video downloader no watermark similar to <a href="/">tiktokio .com</a> tiktok downloader helps user to download tiktok video without watermark, download tiktok story, Tiktok mp3 downloader. Along with the partner <a href="https://insta.com.in" target="_blank" class="text-lg text-gray-900 leading-6 text-left" >Instagram Pro APK</a> offers enhanced features for downloading videos and stories from Instagram.</p>
      						</div>
            			</div>
      			</section>
			</div>
      </div>
</div>



<!--Sention 1 End -->

