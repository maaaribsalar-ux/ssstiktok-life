---
import Container from "@components/container.astro";
import Faq from "@components/faq.astro";
import Features from "@components/tvdfeatures.astro";
import Hero from "@components/tvd-hero.astro";
import Layout from "@layouts/Layout.astro";

// Remove old astro-i18next imports
// import { changeLanguage } from "i18next";
// changeLanguage("en");

// Add new built-in i18n imports
import { getLangFromUrl, useTranslations } from "../i18n/utils";

// Get current language and create translation function
const safeUrl = Astro.url || new URL('/', Astro.site || 'http://localhost:4321');
const lang = getLangFromUrl(safeUrl);
const t = useTranslations(lang);
// Page-specific overrides for title and meta description
const title = "TikTok Video Downloader - Download TikTok Videos Without Watermark Free HD";
const description = "TikTok video downloader to Download TikTok videos free .Save TikTok videos without watermark in HD quality. Fast TikTok video download to all devices!";
---

<Layout title={title} description={description}>
<Hero />
	<Container>
	<Features />
	<Faq />		

	</Container>
</Layout>
